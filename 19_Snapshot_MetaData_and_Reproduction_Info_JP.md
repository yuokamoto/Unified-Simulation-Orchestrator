# 19. スナップショットのグローバルフィールド：再現情報とメタデータ

**Status:** Draft
**Date:** 2026-09-16

> このドキュメントは、[17_Open_Questions_JP.md](./17_Open_Questions_JP.md) の項目7（[18_Snapshot_Alignment_with_Learning_Data_JP.md](./18_Snapshot_Alignment_with_Learning_Data_JP.md) で最初に提起された、「再現情報」（カメラ・照明・Domain Randomization・シード）をどこに持つか）を解決する後日の設計議論の結論を記録するものである。同じ議論で提起された、密接に関連するもう一つの論点（本質的にユーザー任意で復元に不要なデータをスナップショットにどう持たせるか）についても、あわせて決着する。本議論で新たに生じた未解決点は [17_Open_Questions_JP.md](./17_Open_Questions_JP.md) に追加する。

## 論点

[18_Snapshot_Alignment_with_Learning_Data_JP.md](./18_Snapshot_Alignment_with_Learning_Data_JP.md) は、外部の利用者がシミュレートされた状態から画像を再生成したい（毎ステップ画像を保存する代わりに）場合、アセット単位の世界状態を超えた追加情報——カメラの位置・姿勢・内部パラメータ、照明、Domain Randomization 設定、使用した乱数シード（「再現情報」）——が必要になることを明らかにしていた。これをスナップショット形式の内側に持つか、完全に外側（利用者側のメタデータ）に置くかは未決定のまま残されていた。

これと並行して、関連する別の論点も浮上していた：既存の型付きフィールド（アセット単位・グローバルいずれも）に収まらない、動的に変化しうるパラメータの置き場所も必要ではないか。そのために汎用のユーザー定義フィールドを追加すべきか、という論点である。

## 結論

スナップショットの `world` オブジェクトに、`assets` と並ぶ形で新たに2つのフィールドを追加する（スキーマは [03_Snapshot_Specification_JP.md](./03_Snapshot_Specification_JP.md) 参照）：

1. **`reproduction_info`**（グローバル、構造化）— 特定のアセットに紐付かない、シーン全体に関わるレンダリング・再現設定：照明、Domain Randomization 設定、乱数シード。状態から画像そのものを保存せずにレンダリング画像を再生成するという特定の目的にのみ必要。エピソード単位のスコープを持ち、実行中は通常変化しないため、フルスナップショットに乗せる。カメラの位置・姿勢・内部パラメータは意図的に含めない——詳細は下記「カメラはアセットであり、グローバル状態ではない」を参照。
2. **`meta_data`**（グローバル、自由記述）— 復元のためには不要な、ユーザー定義データ。既存のアセット単位 `properties` フィールド（[03_Snapshot_Specification_JP.md](./03_Snapshot_Specification_JP.md)）のグローバル版にあたる。

アセット単位の任意データは、引き続き既存の `properties` フィールドを用いる。`properties` がすでにその役割を果たしているため、新たなアセット単位フィールドは追加しない。

## なぜ `reproduction_info` を `properties` / `meta_data` に統合しないのか

当初の問い方——「スナップショットにあるものは、そもそも再現に必要だから載っているのではないか」——は、「再現」という言葉の異なる2つの意味を混同していた：

1. **シミュレーションの続行に足る復元十分性**：ポーズ、速度、関節状態、状態ラベル、接続関係。これは既存のスナップショットスキーマの大部分（[18_Snapshot_Alignment_with_Learning_Data_JP.md](./18_Snapshot_Alignment_with_Learning_Data_JP.md) の関節状態拡張を含む）がすでに提供しているものであり、画像を再生成するかどうかに関わらず必要である。
2. **その状態から画像を再生成するに足る情報**：カメラ・照明・Domain Randomization・シード。これは、外部利用者が画像を保存せず後から再構成したい場合にのみ関係する、より狭い範囲の追加要件である。

`reproduction_info` が対象とするのは(2)のみであり、それに該当するのは固定された少数のフィールドに限られる——当初見えていたような「スナップショットのほとんど」ではない。これは特定の1機能のために構造的に必須な、限定された一群のフィールドであるため、意図的に無制約で、他の何かから解釈可能であることを一切保証しない `meta_data` とは区別され続ける必要がある。`reproduction_info` を `properties`/`meta_data` に統合してしまうと、この区別が失われ、スキーマだけを見て「利用者が実際に依拠してよいフィールドはどれか」が分からなくなる。

## カメラはアセットであり、グローバル状態ではない

当初の `reproduction_info` の草案には `camera`（姿勢＋内部パラメータ）というサブフィールドが含まれていた。しかし見直してみると、観測に使うカメラそのものは、シーン全体に関わる設定の一種ではなく、他のアセットとまったく同様に「姿勢を持つ物理的な存在」である。2つのケースに分かれる：

- **ロボットに剛体的に取り付けられたセンサー**（例：ロボットの手首カメラ）：その世界座標での姿勢は、親アセットのすでに追跡されている状態——ルート姿勢、および多関節アセットであればすでに追跡されている`joint_positions`——と、アセットのモデル（URDF/USD）で定義された固定の取り付けオフセット・キネマティックチェーンを組み合わせることで常に導出できる。追加のスナップショットフィールドは一切不要である：多関節への取り付けでは導出は関節チェーン全体を通じて行われ、ルートからの単一オフセットだけでは済まないが、それでもすでにスナップショットが持つ状態だけの関数であることに変わりはなく、これを `reproduction_info` に別途重複して持たせると、親アセットの状態とずれうる冗長な状態を作ってしまうだけである。
- **据え置き型の観測カメラ**（例：作業エリアを見下ろす固定の俯瞰カメラ）：これは単なる別のアセット（`type: "camera"`）であり、ロボットやパレットと何ら変わらず、既存のアセット単位の仕組みを通じて `position`/`orientation` を追跡すればよい。

どちらの場合も、内部パラメータ（焦点距離、解像度、画角など）は毎ステップの動的データではなく、そのカメラのモデルの静的な属性であるため、動的なグローバルフィールドではなく、アセットの静的な記述側に属する。その静的な較正情報をどこに・どのような形で表現するかという正確な契約は、まだ定義されていない——項目11参照。

これにより `reproduction_info` に残るのは、真にどのアセットにも属さない設定——照明と Domain Randomization——およびシードのみとなる。これは [11_Design_Principles_JP.md](./11_Design_Principles_JP.md) の原則2の綺麗な適用例でもある：剛体的に取り付けられたセンサーについては、親アセットの追跡済みの姿勢が source of truth であり、カメラの姿勢はそこから導出される量であって、独立した状態として重複して持つべきではない。

## 原則1との関係

この三分割は、[11_Design_Principles_JP.md](./11_Design_Principles_JP.md) の原則1（変わる理由が違うものを分け、その境界を共通の形式でつなぐ）の直接的な適用である：

- アセット単位の動的状態（`position`、`velocity`、`joint_positions`）は、物理・ロジック上の理由で毎シミュレーションステップ変化する。アセット単位の `properties` フィールドも同じスコープ（アセット単位）を持つが、任意のユーザー定義データであるため更新頻度の保証はなく、毎ステップ変化することもあれば、稀にしか、あるいは全く変化しないこともある。
- `reproduction_info` は、レンダリング・観測者設定上の理由で、エピソードにつき多くても1回（通常はリセット時）しか変化しない。
- `meta_data` は、フレームワークの関心の外にある理由で、ユーザーが選ぶ任意の頻度で変化する。

これら3つを統合せず、別々の名前を持つ独立したフィールドとして保つことで、それぞれを独立に扱い、依拠できる状態を保っている。

## 関連ドキュメント

- [03_Snapshot_Specification_JP.md](./03_Snapshot_Specification_JP.md) — `reproduction_info` と `meta_data` のグローバルフィールドを追加したスキーマ
- [11_Design_Principles_JP.md](./11_Design_Principles_JP.md) — ここで適用されている原則1
- [18_Snapshot_Alignment_with_Learning_Data_JP.md](./18_Snapshot_Alignment_with_Learning_Data_JP.md) — 再現情報の要件が最初に顕在化した箇所
- [17_Open_Questions_JP.md](./17_Open_Questions_JP.md) — 本章で解決した項目7、正確なサブフィールド形状は項目8として残す
